<%- include('header') %>
  <main>
    <div class="ui segment">
      <h1>Input Quality Bar for our <a href="<%= boardUrl %>">Trello</a></h1>
      <ul>
        <% cardsAnalysis.forEach(function(card) { %>
          <li>
            <a href="<%= card.url %>">
              <%= card.name %>
            </a>
            <% if (card.sins.length===0) { %>
              <span class="right">is OK</span>
              <% } else { %>
                <span class="wrong">
                  <% card.sins.join(', ') %>
              </span>
            <% } %>
          </li>
          <% }); %>
      </ul>
    </div>
  </main>

<%- include('footer') %>